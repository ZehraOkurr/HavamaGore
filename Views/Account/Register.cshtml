@model HavamaGore.Models.User
@{
    ViewData["Title"] = "Kayıt Ol";
}

<div class="container d-flex align-items-center justify-content-center" style="min-height: 85vh;">
    <div class="glass-card overflow-hidden" style="width: 900px; max-width: 95%;">
        <div class="row g-0">
            
            <div class="col-md-6 d-none d-md-block" style="background-color: #3b4d45;">
                <img src="~/img/rabbit.gif" class="w-100 h-100" style="object-fit: cover; min-height: 550px;" alt="Yağmurda Tavşan">
            </div>

            <div class="col-md-6 p-5 d-flex flex-column justify-content-center">
                <div class="text-center mb-4">
                    <h2 class="fw-bold text-white">Aramıza Katıl 🌱</h2>
                    <p class="small text-white-50">Kendi hava durumu kütüphaneni oluştur.</p>
                </div>

                <form asp-action="Register" method="post">
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-white-50 ms-2">Kullanıcı Adı</label>
                        <input type="text" asp-for="Username" class="form-control" placeholder="Kullanıcı Adı Seç" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-white-50 ms-2">Şifre</label>
                        <input type="password" asp-for="Password" class="form-control" placeholder="Güçlü Bir Şifre" required />
                    </div>
                     <div class="mb-4">
                        <label class="form-label small fw-bold text-white-50 ms-2">Şehir (Varsayılan)</label>
                        <input type="text" asp-for="City" class="form-control" placeholder="Örn: Rize" required />
                    </div>
                    
                    <button type="submit" class="btn btn-primary w-100 rounded-pill shadow-sm">
                        Kayıt Ol ✨
                    </button>
                </form>

                <div class="mt-4 text-center small">
                    Zaten üye misin? <a asp-action="Login" class="fw-bold text-white text-decoration-underline">Giriş Yap</a>
                </div>
            </div>
            
        </div>
    </div>
</div>